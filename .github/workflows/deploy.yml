name: Deploy to AWS Lightsail
on:
  push:
    branches:
      - main  # Adjust this to the branch you want to trigger deployment on

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Check out the repository
      - name: Check out the repository
        uses: actions/checkout@v4

      # Step 2: SSH into Lightsail and deploy
      - name: SSH into Lightsail and deploy
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SSH_HOST }}       # Your Lightsail instance public IP or DNS
          username: ${{ secrets.SSH_USER }}   # Your SSH username (e.g., ubuntu)
          key: ${{ secrets.SSH_PRIVATE_KEY }} # Your SSH private key for authentication
          script: |
            echo "Deploying application to Lightsail instance..."
            cd .
            # Pull the latest Docker image if needed, or build and start your containers
            docker-compose.yml up -d
            docker-compose ps
            docker-compose logs
